# Optional: proxy URL for demo mode (Cloudflare Worker or similar)
# VITE_DEMO_PROXY_URL=https://your-demo-proxy.example.com

# Used only for local dev scripts — never commit real keys
# GEMINI_API_KEY=
